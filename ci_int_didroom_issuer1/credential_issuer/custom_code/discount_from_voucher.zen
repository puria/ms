Given I have a 'number' in path 'authorization_details.claims.discount'
Given I have a 'time' named 'expires_in'

When I create the 'string dictionary' named 'holder_claims'
When I move 'discount' to 'has_discount_from_voucher' in 'holder_claims'

Then print the 'holder_claims'
Then print the 'expires_in'